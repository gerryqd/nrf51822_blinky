Minimal nrf51822 blinky example
===============================

All .h .s .c file (except for main.c) are copied from the SDK.

Use `build.sh` to get the binary.

Use `jlink flash.jlink` to program the flash.

